import React from "react";

function Note(){
    return (
        <div className="note">
            <h1>local</h1>
            <p>Note that the development build is not optimized.</p>
        </div>
    );
}

export default Note;